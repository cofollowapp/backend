
default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  host: localhost

development:
  <<: *default
  database: cofollow_development

test:
  <<: *default
  database: cofollow_test

# production:
#   url: <%= ENV['DATABASE_URL'] %>